services:
  - type: web
    name: arcano-oracle
    runtime: node
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: VITE_GROQ_API_KEY
        sync: false
